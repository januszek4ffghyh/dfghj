{
  "name": "margonem-autowalk-bot",
  "version": "3.1.0",
  "private": true,
  "description": "Margonem AutoWalk bot — loader + hosted core + dev dashboard",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "parse-maps": "node scripts/parse-idmap.js"
  }
}
